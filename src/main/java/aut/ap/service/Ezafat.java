package aut.ap.service;

import aut.ap.model.Email;

import java.util.Arrays;
import java.util.List;

public class Ezafat {



//    برا چند نفر بفرسته
//    List<String> recipients = Arrays.asList(recipientsInput.split("\\s*,\\s*"));
//    send(recipients, subject, body);
//    for (String recipientEmail : recipients) {
//        عملیات سند
//    }





// به ترتیب نشون دادن ایمیل ها
//    List<EmailRecep> recepList = session.createQuery(
//                    "FROM EmailRecep WHERE recipient.id = :userId", EmailRecep.class)
//            .setParameter("userId", currentUser.getId())
//            .getResultList();
//
//            for (int i = 0; i < recepList.size(); i++) {
//        for (int j = i + 1; j < recepList.size(); j++) {
//            Email emailI = session.get(Email.class, recepList.get(i).getEmCode());
//            Email emailJ = session.get(Email.class, recepList.get(j).getEmCode());
//            if (emailI.getDate().isBefore(emailJ.getDate())) {
//                EmailRecep temp = recepList.get(i);
//                recepList.set(i, recepList.get(j));
//                recepList.set(j, temp);
//            }
//        }
//    }


}
